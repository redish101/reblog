basePath: /api/v1
info:
  contact: {}
  description: reblog 后端 api
  title: reblog api
  version: "1.0"
paths:
  /healthz:
    get:
      consumes:
      - application/json
      description: 健康检查
      produces:
      - application/json
      responses:
        "200":
          description: 安然无恙！
          schema:
            type: boolean
      summary: 健康检查
      tags:
      - healthz
swagger: "2.0"
