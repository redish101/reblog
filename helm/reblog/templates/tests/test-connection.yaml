apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "reblog.fullname" . }}-test-connection"
  labels:
    {{- include "reblog.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "reblog.fullname" . }}:{{ .Values.backend.service.port }}']
  restartPolicy: Never
